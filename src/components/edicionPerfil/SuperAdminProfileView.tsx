'use client'

import React from 'react';

export default function SuperAdminProfileView() {
  return (
    <div>
      <h3 className="text-lg font-medium text-[var(--foreground)] mb-2">Perfil SuperAdmin</h3>
      <p className="text-sm text-[var(--muted-foreground)]">Acceso completo y configuraciones críticas. (proximamente)</p>
    </div>
  );
}
