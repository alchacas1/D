'use client'

import React from 'react';

export default function UserProfileView() {
  return (
    <div>
      <h3 className="text-lg font-medium text-[var(--foreground)] mb-2">Perfil de Usuario</h3>
      <p className="text-sm text-[var(--muted-foreground)]">Opciones básicas para usuarios estándar. (proximamente)</p>
    </div>
  );
}
